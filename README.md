- this program was generated via VisualStudio -> generate new CMake program

- use `build.bat` to build, run, etc:
  ```ps1
  .\build.bat build    # configure (if needed) + compile
  .\build.bat run      # run the executable
  .\build.bat init     # re-configure from scratch
  .\build.bat clean    # delete build artifacts
  ```
